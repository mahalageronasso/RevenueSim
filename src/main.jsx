import React from "react";
import ReactDOM from "react-dom/client";
import RevenueSim from "./RevenueSim.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RevenueSim />
  </React.StrictMode>
);
